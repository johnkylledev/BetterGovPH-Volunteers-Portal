{
    "routes": [
        {
            "src": "/api/v1/(.*)",
            "dest": "/api/index?path=$1"
        },
        {
            "src": "/api/(.*)",
            "dest": "/api/index?path=$1"
        }
    ]
}
